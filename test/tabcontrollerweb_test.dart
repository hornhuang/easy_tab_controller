import 'package:flutter_test/flutter_test.dart';
import 'package:tab_controller_web/web_tab_controller.dart';

void main() {
  test('adds one to input values', () {
    final calculator = WebTabController();
  });
}
